interface:
  display_name: "Photo to Clay Style"
  short_description: "Transform photos into soft clay-style 3D models"
  default_prompt: "Use $photo-to-clay-style to transform the uploaded photo into a soft, rounded clay-style 3D model image."

policy:
  allow_implicit_invocation: true
